// Attendance page dummy logic
document.addEventListener("DOMContentLoaded", () => {
  const btn = document.querySelector("#markAttendance");
  btn?.addEventListener("click", () => {
    alert("Attendance marked successfully!");
  });
});
